# must be unique in a given SonarQube instance
sonar.projectKey=gabrielmendessilva_fiap-tech-challenge-soat-ms
# --- optional properties ---
# defaults to project key
sonar.organization=gabrielmendessilva
sonar.projectName=fiap-tech-challenge-soat-ms
# defaults to 'not provided'
sonar.projectVersion=1.0
sonar.javascript.lcov.reportPaths=./coverage/lcov.info
sonar.test.inclusions=**/*.spec.ts
sonar.sources=src/
# Path is relative to the sonar-project.properties file. Defaults to .
#sonar.sources=.
# Encoding of the source code. Default is default system encoding
#sonar.sourceEncoding=UTF-8
# Path is relative to the sonar-project.properties file. Defaults to .
#sonar.sources=.
# Encoding of the source code. Default is default system encoding
#sonar.sourceEncoding=UTF-8
sonar.coverage.exclusions= \
    src/index.ts, \
    src/app.ts, \
    src/adapters/interfaceAdapters/routes/index.ts, \
    src/adapters/interfaceAdapters/routes/swaggerConfig.ts, \
    src/presenters/api/config/server.config.ts, \
    src/presenters/api/index.ts, \
    src/dataSources/database/infra/dbConfig.ts, \